import mwCloudStorageUtils
reload(mwCloudStorageUtils)
from google.cloud import storage

os.environ[
    "GOOGLE_APPLICATION_CREDENTIALS"
] = "M:/offline/key/many-worlds-d48d0252c0e6.json"


remote_path = "236_Andre_ModelBlock.ma"
bucket_name = "manyworlds-test-bucket"

storage_client = storage.Client()
bucket = storage_client.get_bucket(bucket_name)

print storage.Blob(bucket=bucket, name=remote_path).exists(storage_client)