attrs = cmds.listAttr("terraborg_terraborg_RigPuppet_ma:bodySetup_L0_ctl", ud=1)
attrFlag = ""

if len(attrs) > 0:    
    for attr in attrs:
        attrFlag += "-attr " + attr + " "
        
print attrFlag

